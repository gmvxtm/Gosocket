FROM node:22-alpine
WORKDIR /app
ENV NODE_ENV=production

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY src/ src/

ENV HOST=0.0.0.0
EXPOSE 3001
USER node
CMD ["node", "src/server.js"]
