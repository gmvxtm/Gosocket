# Offline Requests

Offline-first request management with deferred synchronization to a central backend.

- `web/` - React client
- `sync-service/` - Node.js service: local persistence, processing and synchronization
- `backend/` - .NET API that registers synchronized requests

Setup and run instructions will be added as the pieces land.
